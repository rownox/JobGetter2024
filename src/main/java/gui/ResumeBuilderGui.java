package gui;

import javax.swing.*;
import java.awt.*;

public class ResumeBuilderGui extends JFrame {
    public ResumeBuilderGui() {
        setLayout(new FlowLayout());
        setTitle("Resume Builder 2023");
        setSize(400, 300);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLocationRelativeTo(null);
        setVisible(true);
    }
}
